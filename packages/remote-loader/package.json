{
  "name": "remote-loader",
  "version": "0.0.0",
  "description": "loads federated-modules",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "npm run clear && tsc --watch",
    "clear": "rimraf dist/",
    "build": "npm run clear && tsc"
  },
  "main": "./dist/index.js",
  "sideEffects": false,
  "dependencies": {},
  "devDependencies": {
    "rimraf": "^3.0.2",
    "tsconfig": "*",
    "typescript": "^4.7.4"
  }
}

{
  "name": "shell",
  "version": "0.0.0",
  "description": "Shell app to load single-spa apps",
  "main": "src/index.tsx ",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "npm run clear && webpack --watch --mode development",
    "clear": "rimraf public/",
    "build": "npm run clear && webpack --mode production"
  },
  "devDependencies": {
    "html-webpack-plugin": "^5.5.0",
    "rimraf": "^3.0.2",
    "ts-loader": "^9.3.1",
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0"
  }
}
